# 开发问题记录

## 第四章：Netlify部署问题排查与解决

### 问题概述

**问题描述**：Netlify部署的网站（https://epmsys37.netlify.app）显示的是旧的静态HTML版本（紫色背景），而不是沙箱中预览的最新Next.js版本（浅蓝色背景）。

**问题时间**：2025年3月9日

**影响范围**：Netlify主域名无法访问最新版本，但预览链接正常

---

### 问题现象

| 项目 | 预期效果 | 实际效果 |
|------|---------|---------|
| 网站背景 | 浅蓝色渐变 | 紫色渐变 |
| 页面标题 | 企业项目管理系统（无emoji） | 🏢 企业项目管理系统（带emoji） |
| 技术栈 | Next.js + React + Tailwind CSS | 纯HTML + CSS |
| 功能 | 完整业务功能（含数据库） | 静态演示版 |

---

### 问题排查过程

#### 第一阶段：诊断本地与远程同步状态

**问题1**：Git GUI推送失败

**原因**：用户当前在 `cloudflare` 分支，而不是 `main` 分支

**解决方案**：
```bash
# 查看当前分支
git branch

# 切换到main分支
git checkout main
```

---

#### 第二阶段：强制推送最新代码

**问题2**：推送显示"Everything up-to-date"

**原因**：本地和远程的提交ID相同（都是 `2414685`），没有新的变更

**诊断命令**：
```bash
# 查看本地和远程的提交
git log --oneline -1 main
git log --oneline -1 origin/main

# 对比提交ID
git rev-parse main
git rev-parse origin/main
```

---

#### 第三阶段：发现public/index.html文件

**问题3**：`public/index.html` 文件仍然存在

**原因**：这个旧的静态HTML文件没有被删除，Netlify会优先使用这个文件

**错误尝试**：
- 在GitHub网页上手动删除 `public/index.html` → Netlify重新部署后仍然显示旧版
- 创建空提交触发部署 → 无效
- 清除Netlify缓存部署 → 无效

**正确做法**：
```bash
# 使用git rm删除文件
git rm public/index.html
git commit -m "fix: 删除旧的静态HTML文件"
git push origin main
```

---

#### 第四阶段：完全重置项目

**问题4**：部分文件可能版本不一致

**解决方案**：完全重置项目为沙箱最新版本

**操作步骤**：
```bash
# 1. 清理旧文件（保留.git）
rm -rf src public node_modules .next
rm -f package.json next.config.* drizzle.config.*

# 2. 从解压缩的projects文件夹复制所有新文件

# 3. 添加并提交
git add -A
git commit -m "feat: 完整重置项目为最新版本"

# 4. 强制推送
git push -f origin main
```

**推送结果**：
```
新提交ID: f8627f6
提交信息: "feat: 完整重置项目为最新版本"
推送成功: 从 2a2bb93 更新到 f8627f6
```

---

#### 第五阶段：发现真正问题

**问题5**：Netlify最新部署成功，但主域名未更新

**现象**：
- ✅ 预览链接正常：`https://69afe75bffb6b20007d07ec3--epmsys37.netlify.app/`
- ❌ 主域名未更新：`https://epmsys37.netlify.app`

**原因**：Netlify的最新部署没有**发布（Publish）**到主域名

---

### 最终解决方案

#### 在Netlify控制台发布最新部署

**操作步骤**：

1. 登录Netlify控制台：https://app.netlify.com
2. 进入项目 `epmsys37`
3. 点击 **Deployments** 标签
4. 找到最新的部署（显示正确内容的那个）
5. 点击右侧的 **"..."**（三个点）菜单
6. 选择 **"Publish deploy"**
7. 确认发布
8. 等待1-2分钟后访问主域名验证

**操作示意图**：
```
Netlify Deployments 页面
┌────────────────────────────────────────────┐
│ Deploy #1 (最新) ✓ Production             │
│ https://69afe75bffb6b20007d07ec3--...      │
│ [Preview] [...] ← 点击这里                 │
│                                             │
│ Deploy #2 (旧)                             │
│ https://epmsys37.netlify.app               │
└────────────────────────────────────────────┘

点击 [...] → "Publish deploy"
```

---

### 问题总结

#### 根本原因

Netlify的部署流程包含三个阶段：
1. **Building**：构建新版本 ✅
2. **Deploy preview**：生成预览链接 ✅
3. **Publish**：发布到主域名 ❌（需要手动操作）

#### 关键发现

1. **Git分支混淆**：用户在cloudflare分支操作，而不是main分支
2. **旧文件残留**：`public/index.html` 文件没有正确删除
3. **Netlify发布机制**：最新部署需要手动发布到主域名

#### 经验教训

1. **确认当前分支**：操作前先用 `git branch` 确认在正确的分支
2. **使用git rm删除文件**：不要在文件管理器直接删除，要用 `git rm`
3. **检查Netlify发布状态**：部署成功不等于已发布到主域名
4. **使用预览链接测试**：先确认预览链接正确，再发布到主域名

---

### 相关命令速查

```bash
# Git分支操作
git branch                    # 查看当前分支
git checkout main            # 切换到main分支
git checkout -b main         # 创建并切换到main分支

# Git文件操作
git rm public/index.html     # 删除文件
git add -A                   # 添加所有文件
git commit -m "message"      # 提交更改

# Git推送操作
git push origin main         # 推送到远程
git push -f origin main      # 强制推送

# Git诊断命令
git status                   # 查看状态
git log --oneline -5         # 查看最近5次提交
git rev-parse main           # 查看提交ID
git diff main origin/main    # 对比本地和远程
```

---

### Netlify配置检查清单

#### Build设置
- **Build command**: `pnpm run build` 或 `npm run build`
- **Publish directory**: `.next` 或留空（让Netlify自动检测）
- **Node version**: 在 `netlify.toml` 或环境变量中设置

#### 环境变量
- `DATABASE_URL`: PostgreSQL数据库连接字符串（如果使用数据库）
- 其他API密钥和配置

#### Next.js配置
- 确认 `next.config.mjs` 中**没有** `output: 'export'`（如果有则移除）
- 使用服务端渲染（SSR）而不是静态导出

---

### 参考链接

- GitHub仓库：https://github.com/WastonXH/enterprise-project-system
- Netlify主域名：https://epmsys37.netlify.app
- Netlify控制台：https://app.netlify.com
- 项目代码下载：`enterprise-project-full.tar.gz`（已上传到对象存储）

---

### 后续工作

#### 数据库配置（待完成）

Netlify部署成功后，需要配置数据库环境变量：

1. 在Netlify控制台 → Site settings → Environment variables
2. 添加以下变量：
   ```
   DATABASE_URL=postgresql://用户名:密码@主机:端口/数据库名
   ```
3. 触发重新部署

#### 功能测试清单

- [ ] 首页显示正确（浅蓝色背景，四个部门卡片）
- [ ] 业务部表单正常（自动编号、打印、保存）
- [ ] 研发部配置正常（玻璃/IC选型）
- [ ] 采购部资源库展示正常
- [ ] 质量部记录表单正常
- [ ] 数据库连接正常

---

## 附录：双版本并存策略

### 架构说明

```
main分支        → Netlify部署（完整版本）
                   └─ Next.js + PostgreSQL + API Routes
                   
cloudflare分支  → Cloudflare Pages部署（静态版本）
                   └─ Next.js静态导出（无数据库、无API）
```

### 使用场景

| 版本 | 地址 | 用途 | 访问速度 | 功能完整性 |
|------|------|------|---------|-----------|
| Netlify | https://epmsys37.netlify.app | 内部员工使用 | 较慢（可能需要代理） | 100% |
| Cloudflare | https://xxx.pages.dev | 展示和快速访问 | 快（中国大陆友好） | 30% |

### 维护策略

1. **main分支**：继续开发新功能，保持完整功能
2. **cloudflare分支**：定期从main分支同步UI更新，保持静态版本

---

**记录时间**：2025年3月9日  
**记录人**：Coze AI助手  
**问题状态**：✅ 已解决

---

# 第五章：2月7日-3月7日问题汇总

详细内容请查看：[开发问题记录_第5章.md](开发问题记录_第5章.md)

### 📅 时间范围
2025年2月7日 - 2025年3月7日

---

## 📊 问题总览

| 序号 | 问题类型 | 发现时间 | 解决时间 | 严重程度 | 状态 |
|------|---------|---------|---------|---------|------|
| 1 | 业务部表单功能缺失 | 2月7日 | 2月7日 | 高 | ✅ 已解决 |
| 2 | 研发部表单交互问题 | 2月8日 | 2月9日 | 高 | ✅ 已解决 |
| 3 | Netlify预览链接失效 | 2月11日 | 2月11日 | 中 | ✅ 已解决 |
| 4 | 业务部缺少打印功能 | 2月11日 | 2月11日 | 中 | ✅ 已解决 |
| 5 | Vercel部署配置问题 | 2月24日 | 2月24日 | 高 | ✅ 已解决 |
| 6 | Vercel访问超时 | 2月25日 | 2月26日 | 高 | ✅ 已解决 |
| 7 | Cloudflare Pages兼容性 | 2月27日 | 3月4日 | 高 | ✅ 已解决 |
| 8 | Netlify主域名未更新 | 3月9日 | 3月10日 | 高 | ✅ 已解决 |

---

## 🔍 关键问题摘要

### 问题1：业务部表单功能缺失
- **解决方式**：分步骤完善表单字段，添加产品结构、项目等级、技术类别等
- **提交数量**：10+次提交
- **经验教训**：表单字段应该在一开始就完整规划

### 问题2：研发部表单交互问题
- **解决方式**：重构触控及盖板配置，实现二选一逻辑和多选特性
- **关键提交**：`6750d04` - feat: 重构研发部触控及盖板配置
- **经验教训**：复杂交互逻辑应该先用简单版本测试

### 问题6：Vercel访问超时
- **解决方式**：采用双版本策略（main分支→Netlify完整版，cloudflare分支→Cloudflare静态版）
- **根本原因**：Vercel服务器在中国大陆访问不稳定
- **经验教训**：生产环境部署应该考虑目标用户的地理位置

### 问题8：Netlify主域名未更新
- **解决方式**：在Netlify控制台手动点击"Publish deploy"
- **根本原因**：Netlify部署成功后不会自动发布到主域名
- **经验教训**：部署成功 ≠ 已发布到主域名

---

## 📈 问题统计

### 按问题类型分类
- 功能缺失：2个（25%）
- 组件错误：1个（12.5%）
- 部署配置：3个（37.5%）
- 访问问题：2个（25%）

### 按解决时长分类
- 当天解决：5个（62.5%）
- 1-3天：2个（25%）
- 3天以上：1个（12.5%）

---

**完整问题记录请查看**：[开发问题记录_第5章.md](开发问题记录_第5章.md)
